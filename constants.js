const IP = 'localhost';
const PORT = 50541;
const welcomeMessage = 'You have sucessfully connected to the game server!';
const stdin = process.stdin;

module.exports = {
  IP,
  PORT,
  welcomeMessage,
  stdin
};

